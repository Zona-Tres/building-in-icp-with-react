{
  "name": "react-to-ic",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "rm -rf .azle && rm -rf .dfx && rm -rf .turbo && rm -rf node_modules"
  },
  "dependencies": {
    "azle": "^0.19.0"
  },
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
